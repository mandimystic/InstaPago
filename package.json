{
  "name": "instapago-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=dev nodemon index.js",
    "start": "NODE_ENV=production node index.js",
    "build:ui": "rm -rf dist && cd ../instapago-front/ && npm run build && cp -r dist ../instapago-backend"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cross": "^1.0.0",
    "env": "^0.0.2",
    "eslint": "^9.1.1",
    "nodemon": "^3.1.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-static-gzip": "^2.1.7",
    "nodemailer": "^6.9.13"
  }
}
